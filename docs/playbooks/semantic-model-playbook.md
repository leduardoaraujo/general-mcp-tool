# Semantic Model Playbook

Tags: power_bi, semantic_model, dax, sales

For Power BI requests, prefer the semantic model when the user asks about governed metrics, measures or business KPIs.

Use DAX-style reasoning for measures such as Total Sales and Gross Margin.

Do not bypass the semantic model unless the request explicitly asks for raw SQL.
